import com.view.au.UserView;
/**
 * In the main class, we run only the parameters of the first authorization menu
 *
 * @author Vasiliy
 * @version 1.0
 * */
public class Auction {
    public static void main(String[] args) {
        UserView userView = new UserView();
        userView.UserView();

    }
}
